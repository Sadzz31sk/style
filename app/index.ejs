<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Styles Spectrum</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="css/style.css">
</head>
<body class="flex flex-col w-full min-h-screen bg-white overflow-scroll">
    <header class="flex flex-col gap-1 justify-center h-[100px] items-center w-full">
        <h1 class="text-3xl font-bold">Styles <span class="text-purple-600">Spectrum</span></h1>
    </header>
    
    <div class="w-[90vw] m-auto py-3 px-2 min-h-[90vh] flex flex-col gap-4 items-center">
        <div class="w-full flex max-sm:flex-col gap-4">
            <!-- Select Model -->
            <div class="w-[33%] max-sm:w-full bg-gray-200 p-4 rounded-md flex flex-col items-center">
                <label class="text-sm font-semibold">Select Model</label>
                <select id="modelSelect" class="mt-2 p-2 border rounded">
                    <option value="male.jpg">Male</option>
                    <option value="female.jpg">Female</option>
                </select>
                <img id="modelPreview" src="assets/images/male.jpg" alt="Model Preview" class="w-full h-auto max-h-[400px] mt-4 rounded object-contain">
            </div>
            
            <!-- Upload Design -->
            <div class="w-[33%] max-sm:w-full flex flex-col bg-white border-2 border-dashed border-gray-400 p-6 text-center rounded-md hover:border-violet-500 transition">
                <p class="text-sm font-semibold">Upload Design</p>
                <label for="fileUpload" class="flex flex-col flex-1 justify-center gap-1 mt-4 p-4 border border-gray-300 rounded cursor-pointer transition">
                    <p class="text-gray-600">Drag & Drop your design</p>
                    <p class="text-blue-600 cursor-pointer">or Browse</p>
                    <input type="file" id="fileUpload" class="hidden" accept="image/*" onchange="previewUpload()">
                </label>
                <img id="uploadPreview" src="" alt="Uploaded Design Preview" class="w-full h-auto max-h-[400px] mt-4 rounded object-contain hidden">
            </div>

            <!-- Garment Description & Generate Button -->
            <div class="w-[30%] max-sm:w-full flex flex-col gap-4">
                <div>
                    <label for="garmentDescription" class="text-sm font-semibold block mb-2 ml-1">Garment Description</label>
                    <textarea id="garmentDescription" rows="4" class="w-full p-3 border rounded-md focus:outline-none focus:ring-2 focus:ring-violet-500" placeholder="Enter details about the garment..."></textarea>
                </div>
                <button id="callApiButton" class="px-6 py-2 bg-purple-600 text-white rounded-md hover:bg-purple-700 transition">Generate Design</button>
            </div>
        </div>

        <!-- API Image Display Box -->
        <div class="w-full min-h-[60vh] bg-gray-300 mt-4 flex items-center justify-center rounded-md relative">
            <p id="placeholderText" class="text-lg text-gray-700 absolute">Generated design will be displayed here</p>
            <img id="apiImage" src="" alt="Generated Design" class="max-w-full max-h-[400px] object-contain hidden">
        </div>
    </div>

    <script src="js/app.js"></script>
</body>
</html>
